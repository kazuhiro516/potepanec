<!-- LIGHT SECTION -->
<%= provide(:title, @taxon.name) %>
<%= render partial: 'partial/light_section', locals: {title: @taxon.permalink} %>
<!-- MAIN CONTENT SECTION -->
<section class="mainContent clearfix productsContent">
  <div class="container">
    <div class="row">
      <div class="col-md-3 col-sm-4 col-xs-12 sideBar">
        <%= render 'taxonomies' %>
        <%= render 'search' %>
        <div class="col-md-9 col-sm-8 col-xs-12">
          <%= render 'switch' %>
          <%= render 'products' %>
        </div>
      </div>
    </div>
  </div>
</section>
<!-- BRAND LIGHT SECTION -->
<%= render 'brand_light_section' %>
